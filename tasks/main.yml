---
- name: (Debian) install...
  when: not sbt_native and ansible_os_family == 'Debian'
  include: Debian.yml

- name: (RedHat) install...
  when: not sbt_native and ansible_os_family == 'RedHat'
  include: RedHat.yml
  
- name: (Native) install...
  when: sbt_native
  include:  Native.yml
